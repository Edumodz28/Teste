local Lib = loadstring(game:HttpGet("https://raw.githubusercontent.com/7yhx/kwargs_Ui_Library/main/source.lua"))()

local UI = Lib:Create{
    Theme = "Dark", -- ou qualquer outro tema
    Size = UDim2.new(0, 0, 0, 0) -- padrão
}


 
 
 local QuitDivider = Main:Divider{
    Name = "Sair"
 }

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")

local speeds = {16, 32, 64, 128, 256, 512, 1024, 2048}
local currentSpeedIndex = 1

UserInputService.InputBegan:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.V then
        currentSpeedIndex = currentSpeedIndex % #speeds + 1
        local newSpeed = speeds[currentSpeedIndex]
        humanoid.WalkSpeed = newSpeed
        print("Velocidade Alterada para " .. newSpeed)
    elseif input.KeyCode == Enum.KeyCode.L then
        UI:Close()
    end
end)

-- Configuração inicial da velocidade
humanoid.WalkSpeed = speeds[currentSpeedIndex]
